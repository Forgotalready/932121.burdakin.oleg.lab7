function drawRegularPolygon(vertexNum)
{
    const canvas = document.querySelector(".canvas")
    const context = canvas.getContext("2d")
    context.fillStyle = "green"
    context.fillRect(10, 10, 50, 50)
}